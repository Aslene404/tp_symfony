{% extends 'base.html.twig' %}

{% block title %}Hello {{controller_name}}{% endblock %}


{% block body %}

<section class='articles'> {% for article in articles %} <article> <h2>{{article.titre}} </h2> <div class="metadata">Edité le {{article.createdAt | date("m/d/Y")}} à {{article.createdAt | date("h/i")}}</div> <div class="content"> <img src="{{article.image}}" alt=""> {{article.contenu | raw }} <a href="{{ path('blog_show' , {id: article.id}) }}" class="btn btn-primary">Lire la suite</a> 
<a href="{{ path('blog_edit' , {id: article.id}) }}" class="btn btn-success">Modifier</a></div> </article> {% endfor %} </section>
{% endblock %}
